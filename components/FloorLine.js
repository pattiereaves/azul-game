import { PureComponent } from 'react';

export default class FloorLine extends PureComponent {
  state = {
    tiles: [],
  };

  render() {
    return (
      <div>This is the floor line.</div>
    );
  }
}
